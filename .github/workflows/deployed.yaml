name: cloning git code 
on: [push]
jobs:
  cloning_git: 
    runs-on: ubuntu-latest 
    steps: 
      - name: "cloning git  code" 
        run: |
          echo "clone code from remoite repo" 
          echo "print git logs"
          echo "${{tojson(github)}}"
          git clone https://github.com/${{github.repository}}.git

  

            
      
      
  
